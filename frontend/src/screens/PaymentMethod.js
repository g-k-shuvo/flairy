import React from "react";
import PaymentMethodContainer from "../components/PaymentMethod/PaymentMethodContainer";

const PaymentMethod = ({ history }) => {
  return <PaymentMethodContainer history={history} />;
};

export default PaymentMethod;
