import React from "react";
import PlaceOrderContainer from "../components/PlaceOrder/PlaceOrderContainer";

const PlaceOrder = ({ history }) => {
  return <PlaceOrderContainer history={history} />;
};

export default PlaceOrder;
