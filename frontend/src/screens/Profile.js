import React from "react";
import ProfileContainer from "../components/Profile/ProfileContainer";

const Profile = ({ location, history }) => {
  return <ProfileContainer location={location} history={history} />;
};

export default Profile;
