import React from "react";
import UserListContainer from "../components/UserList/UserListContainer";

const UserList = ({ history }) => {
  return <UserListContainer history={history} />;
};

export default UserList;
