import React from "react";
import OrderListContainer from "../components/OrderList/OrderListContainer";

const OrderList = ({ history }) => {
  return <OrderListContainer history={history} />;
};

export default OrderList;
