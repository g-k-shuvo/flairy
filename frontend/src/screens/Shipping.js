import React from "react";
import ShippingContainer from "../components/Shipping/ShippingContainer";

const Shipping = ({ history }) => {
  return <ShippingContainer history={history} />;
};

export default Shipping;
